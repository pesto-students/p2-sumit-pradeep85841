describe("Calculator tests", () => {
     test('adding a + b should return a+b', () => {
   var result = mathOperations.sum(a,b)
    expect(result).toBe(a+b);
 });
      test('adding a - b should return a-b', () => {
   var result = mathOperations.sum(a,b)
    expect(result).toBe(a-b);
 });
      test('adding a * b should return a*b', () => {
   var result = mathOperations.sum(a,b)
    expect(result).toBe(a*b);
 });
})